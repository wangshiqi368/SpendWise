# SpendWise 项目阶段总结报告

## 1. 项目概览

SpendWise 是一款采用现代化 Android 开发标准打造的个人财务管理（PFM）应用。该项目不仅解决了基础的个人记账需求，更通过深入的用户体验打磨与复杂架构设计，演进为一个兼具“数据绝对隐私”、“多媒体留证”以及“全球化跨币种支持”的专业级智能账本。

本报告对迄今为止的开发成果、技术沉淀以及未来演进路线进行全面总结。

---

## 2. 技术架构与选型

项目严格遵循了 Google 官方推荐的最新 Android 开发范式，确保了代码的高可维护性与高扩展性。

*   **表现层 (Presentation Layer)**：
    *   **UI 框架**：全面采用声明式框架 **Jetpack Compose** (Material 3)，实现了包含动画饼图、底部弹窗、动态进度条在内的丝滑交互。
    *   **架构模式**：采用 **MVVM**，利用 `StateFlow` 和 `SharedFlow` 实现单向数据流（UDF）。
    *   **导航**：使用 **Navigation Compose** 实现类型安全的屏幕流转与参数传递。
*   **领域层 (Domain Layer)**：
    *   应用了 **Clean Architecture** 思想，提炼出了如 `GetCategoryStats`、`CurrencyConverter` 等大量纯 Kotlin 的 Use Case，完全解耦于 Android 框架。
*   **数据层 (Data Layer)**：
    *   **本地存储**：使用 **Room Database**，并在开发过程中成功执行了多次平滑的 Migration（v1 -> v5）。
    *   **网络通信**：接入 **Retrofit + OkHttp** 架构，用于拉取实时汇率。
    *   **图片持久化**：使用 **Coil** 结合系统 API，将用户附件沙盒化、私有化存储，保障物理凭证的绝对安全。
*   **依赖注入与构建**：
    *   采用 **Hilt** 进行全局和模块化的依赖注入。
    *   使用 **Gradle Version Catalog (libs.versions.toml)** 统一管理项目依赖。

---

## 3. 已实现的核心与进阶功能

目前，SpendWise 已经具备了一套极具竞争力的功能矩阵：

### 3.1 核心记账闭环 (Core CRUD)
*   **极致输入体验**：键盘主导的记账面板，结合水平滑动的 Emoji 分类选择器。
*   **动态列表展示**：支持历史账单的浏览、基于关键词的实时全文检索以及安全的二次确认删除。
*   **时间维度追踪**：顶部的自定义月份选择器，可自由穿梭查看历史数据的分布。

### 3.2 进阶财务管控
*   **月度预算监控**：支持为特定月份设定预算，首页看板通过“绿-橙-红”变色进度条提供实时的视觉预警机制。
*   **支出统计与分析**：通过纯手工 Compose Canvas 绘制的动态饼图，将杂乱的流水聚合为按金额倒序的图文占比列表。

### 3.3 核心护城河与差异化能力
*   **多币种与实时汇率**：支持 CNY, USD, EUR, JPY 等多种货币的独立记账。底层网络层已接入实时汇率 API，无论使用何种货币记账，首页与报表均可自动折算为本位币（CNY）进行准确统计。
*   **多媒体附件（收据留证）**：用户可以为单笔交易附加图片或小票照片。应用会自动将其拷贝至内部私有空间长期保存，配合缩略图和全屏预览，实现了从“纯记账”到“财务留证”的跨越。
*   **隐私与安全背书**：支持无网环境下的汇率本地回退缓存。同时，提供了一键将全量数据导出为 CSV 文件并利用原生 ShareSheet 进行分享备份的功能，贯彻了“数据属于用户”的纯净产品理念。

---

## 4. 质量保障与测试情况

项目不仅完成了功能堆叠，还构建了一套行之有效的质量保障体系：

*   **自动化单元测试 (Unit Tests)**：
    *   引入了 `MockK` 和 `kotlinx-coroutines-test`。
    *   核心汇率计算引擎 (`CurrencyConverter`)、核心拦截校验逻辑（如 `AddTransaction`）、统计分组算法，以及 ViewModel 的状态机流转均有详尽的 Test Case 覆盖。
    *   目前所有单元测试在本地执行均 **100% 编译并通过**。
*   **端到端业务测试计划 (E2E Test Plan)**：
    *   输出了一份标准的 `TEST_PLAN.md` 业务测试矩阵，覆盖了边界金额、断网策略、空状态渲染、跨币种逻辑等 16 项关键测试用例。

---

## 5. 未来开发计划 (Roadmap & Next Steps)

根据前期的竞品分析以及阶段 1.1 和 1.2 的落地，项目接下来的开发重心将完全集中于“数据洞察”与“AI 黑科技”。

### 阶段二：全球化报表可视化深度升级 (Phase 2)
*   **多维度切换**：在当前的 `StatisticsScreen` 中引入 TabRow 视图。
*   **币种柱状图**：除现有的“按类别（餐饮/交通）”外，新增“按币种（USD/CNY）”消费金额的横向或纵向柱状图（Bar Chart）展示，深化多币种功能的应用场景。

### 阶段三：终极护城河 - AI OCR 小票智能识别 (Phase 3)
*   **端侧 AI 集成**：引入免费的 `Google ML Kit (Text Recognition)` 模型，且保证全程离线识别以维护隐私。
*   **智能填单**：开发正则和文本清洗算法。用户只需拍摄购物小票，应用将自动在后台抓取金额数字和可能存在的货币符号，实现记账页面的自动化填单，从而彻底解决记账阻力的“最后一公里”问题。

### 其它储备方向 (Backlog)
*   支持深浅色模式的动态系统跟随切换优化。
*   实现 Material 3 的动态取色 (Dynamic Color) 以提升沉浸感。
