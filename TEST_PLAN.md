# SpendWise 业务流程测试计划 (Test Plan)

本文档旨在为 SpendWise 应用提供一个全面的、覆盖所有核心业务流程的端到端（E2E）手动测试指南。

## 1. 测试概览

*   **测试目标**：验证应用的核心记账闭环、进阶财务管理功能以及多媒体交互的稳定性和准确性。
*   **测试环境建议**：
    *   一台运行 Android 8.0 (API 26) 及以上的 Android 手机或模拟器。
    *   提前准备几张图片用于附件测试。

---

## 2. 核心记账业务流程 (Core CRUD)

### 2.1 账单的增删改查
*   **[TC-01] 新增单笔账单**
    *   **步骤**：点击首页 "+" 按钮 -> 输入标题（如"午餐"）、金额（如"25.5"） -> 选择"餐饮"分类 -> 点击保存。
    *   **预期**：自动返回首页，列表顶部出现新添加的账单，首页汇总金额增加 25.5。
*   **[TC-02] 异常金额与空白标题校验**
    *   **步骤**：在记账页将标题留空或金额设为 0，点击保存。
    *   **预期**：底部弹出 Snackbar 提示"标题不能为空"或"金额必须大于0"，账单未被保存。
*   **[TC-03] 编辑已有账单**
    *   **步骤**：点击列表中的某笔账单 -> 将金额修改为新数值，更换分类 -> 保存。
    *   **预期**：返回首页，列表项显示新数值和新图标，汇总金额正确重新计算。
*   **[TC-04] 删除账单与防误触机制**
    *   **步骤**：点击账单右侧的垃圾桶图标 -> 在弹窗中点击"取消" -> 再次点击垃圾桶，点击"删除"。
    *   **预期**：第一次点击取消后列表无变化；第二次点击删除后，该账单从列表中消失，汇总金额扣除该笔数值。

### 2.2 多媒体附件 (Image Attachments)
*   **[TC-05] 添加与预览附件**
    *   **步骤**：记账时点击"上传图片" -> 从相册选择一张照片 -> 页面显示出照片缩略图 -> 保存。
    *   **预期**：首页列表中该笔账单出现"曲别针"附件图标；再次点击进入编辑，能正确加载原图缩略图。
*   **[TC-06] 移除附件**
    *   **步骤**：进入带有附件的账单编辑页 -> 点击图片右上角的"X"按钮 -> 保存。
    *   **预期**：回到首页，"曲别针"图标消失；再次进入编辑页，显示为"上传图片"按钮。

---

## 3. 进阶功能与财务分析 (Advanced Features)

### 3.1 跨币种支持与自动换算 (Multi-Currency)
*   **[TC-07] 切换币种记账**
    *   **步骤**：记账时，点击金额旁边的币种按钮 -> 选择 "USD $" -> 输入金额 "100" -> 保存。
    *   **预期**：列表中该笔账单显示为 "$ 100.00"；首页的"本月总支出"自动将该笔美元按照预设汇率（如 7.24）换算为 724.00 CNY 进行累加。

### 3.2 月度预算与看板 (Budget Progress)
*   **[TC-08] 首次设置与修改预算**
    *   **步骤**：首页点击看板区的 "+ 设置月度预算" -> 输入 "1000" -> 确定。
    *   **预期**：看板出现进度条。
*   **[TC-09] 进度条颜色预警**
    *   **步骤**：不断新增账单，使其总支出达到预算的 70% 和 90% 以上。
    *   **预期**：进度条颜色在达到 70% 时变为橙色（警告），达到 90% 时变为红色（危险）。

### 3.3 时间维度与跨月查看 (Month Filtering)
*   **[TC-10] 切换查看历史月份**
    *   **步骤**：点击首页顶部的当前月份（如 "2026-05"） -> 切换到上个月 -> 确定。
    *   **预期**：列表变为空（若上月无数据）；看板金额重置为 0；预算显示为该月的独立预算配置。在此页面记账，账单自动归属到所选月份。

### 3.4 搜索与实时筛选 (Search)
*   **[TC-11] 关键词与分类检索**
    *   **步骤**：在首页搜索框输入关键词（如"午餐"或"餐饮"）。
    *   **预期**：列表实时过滤出匹配标题或分类的账单，同时汇总看板的"总支出"动态更新为搜索结果的汇总额。

### 3.5 统计图表 (Statistics)
*   **[TC-12] 饼图渲染与分类统计**
    *   **步骤**：添加多笔涵盖不同分类的账单 -> 点击右上角"饼图"图标进入统计页。
    *   **预期**：页面显示展开动画的环形饼图；下方列表按支出金额从大到小排列各个分类，各项占比相加为 100%。

### 3.6 数据导出共享 (CSV Export)
*   **[TC-13] 导出 CSV 报表**
    *   **步骤**：在首页点击右上角"分享"图标。
    *   **预期**：唤起系统原生分享面板，文件名为 `SpendWise_Export_时间戳.csv`，通过微信或文件管理器保存后，能看到所有账单明细（含转义处理的标题/备注）。

---

## 4. UI/UX 边界测试 (Edge Cases)

*   **[TC-14] 深浅色模式适配**
    *   **步骤**：在手机系统设置中切换为"深色模式"（Dark Mode），打开 SpendWise。
    *   **预期**：界面背景变深，文字变为浅色；饼图颜色依旧鲜艳，看板与进度条颜色无突兀感，可正常阅读。
*   **[TC-15] 完美空状态展示**
    *   **步骤**：清空所有账单，或在搜索框输入一个不存在的极长乱码。
    *   **预期**：列表区域显示提示文字（如 "还没有账单记录" 或 "未找到匹配的账单"）以及相应的 Emoji 图标，代替空白页面。
*   **[TC-16] 极端金额测试**
    *   **步骤**：记入一笔 "999999999.99" 的金额。
    *   **预期**：列表及看板能正常显示而不出现 UI 越界或科学计数法崩溃。
